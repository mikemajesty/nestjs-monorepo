// common jest init
// only common jest envs and mocks initialization
jest.setTimeout(2000);

process.env.ENV = 'test';





// -----------------------------------------------------------MOCK----------------------------------------------------------------//