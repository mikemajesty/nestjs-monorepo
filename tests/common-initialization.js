// common jest init
// only common jest env and common initialization
jest.setTimeout(2000);

process.env.ENV = 'test';





// -----------------------------------------------------------MOCK----------------------------------------------------------------//