#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../thriftrw/thrift2json.js" "$@"
else
  exec node  "$basedir/../thriftrw/thrift2json.js" "$@"
fi
